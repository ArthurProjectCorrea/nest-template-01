# Porta do servidor HTTP (padrão: 3000)
PORT=8000

# Adicione abaixo as variáveis de ambiente do seu projeto.
# Copie este arquivo para .env e preencha os valores.
# O .env não é versionado (está no .gitignore).
